(ns com.ambrosebs.schema-incubator.poly.other-namespace
  ;schema.other-namespace
  "Code for tests that need to setup things in a different
  namespace."
  (:require [schema.core :as s]))

(defmulti ef408750 identity)
